/**
 * ToolSettings — tool credential/toggle management.
 *
 * Note: The full tool management UI (expandable cards, health indicators,
 * credential modals, per-function toggles) lives in src/pages/Tools.tsx (TM3).
 * This component provides a navigation pointer from Settings to the Tools page
 * and is a placeholder for any settings-level tool configuration that may be
 * added in future phases.
 */
export function ToolSettings() {
  return null;
}
