# Project Health Brief -- 2026-03-09

**Score: 3/10**
Starting at 5: +0 (Wave 15C not complete -- 3/6 phases done), -1 (QA FAIL on iOS8), +1 (zero critical findings open), -1 (app security warn: refresh-failure token cleanup, API key in UserDefaults), -1 (infra N/A mid-wave -- carried from previous brief), +1 (E2E exists from PW1-PW3), -1 (>25% of current wave phases pending -- 3/6 remaining). Clamped to 3/10.

## What Happened (since last brief)
1. iOS8 implementation delivered: dual-provider transcription (OpenAI Whisper + local whisper.cpp), AudioRecorderService, AudioPlayerService, VoiceService, VoiceViewModel, VoiceRecordButton, ComposerBar voice integration, TranscriptionProviderView in Settings. 38 Python tests pass.
2. QA FAIL: Swift test target does not compile. MockAudioRecorder missing 3 required protocol properties (`isRecording`, `duration`, `audioLevel`). All 15 Swift tests are untestable. Claimed "112 tests pass" is false.
3. New standalone `tools/whisper-service/server.py` -- a well-implemented FastAPI wrapper for whisper.cpp binary, but not wired into Docker Compose or any project automation.

## Greatest Risk
**Swift test debt is compounding.** iOS8 is the first phase where tests actually fail to compile (not just missing coverage). The root cause is trivial (3 missing mock properties), but the pattern is concerning: the developer claimed "112 total pass" without running `swift test`. If mock conformance errors slip through on a simple phase like this, more complex phases (iOS9 offline queue, iOS11 integration) are likely to produce subtler compilation or runtime issues. Combined with the existing "no app target" risk from iOS7, the Swift codebase is accumulating untested wiring at an accelerating rate.

## Decisions Needed
- Fix the two blocking issues (MockAudioRecorder conformance + whisper-service documentation) and resubmit for QA cycle 2.
- Consider whether `tools/whisper-service/` should be part of Docker Compose or remain a host-only development tool with its own documentation.

## Security Posture -- Application
| Area | Status | Detail |
|------|--------|--------|
| Auth | ok | Bearer token on voice uploads, 401 surfaced as typed error |
| Secrets | warn | API key in UserDefaults (not Keychain) in TranscriptionProviderView; `_DEV_SECRET` fallback persists |
| Domain isolation | ok | iOS is pure client; no cross-domain imports |
| Input validation | ok | Audio validation (25MB, MIME types), provider name validation (ValueError on unknown) |
| Error handling | warn | Refresh failure still does not clear stale Keychain tokens; `try? configurePlaybackSession()` swallows errors |

## Security Posture -- Infrastructure
N/A -- mid-wave

| Area | Status | Detail |
|------|--------|--------|
| Claude Code permissions | N/A | Mid-wave |
| Docker config | N/A | Mid-wave |
| CORS / network exposure | N/A | Mid-wave |
| Secrets in repo | N/A | Mid-wave |
| Dependency pinning | N/A | Mid-wave |

## Risks You Are Taking
1. **Swift tests do not compile.** iOS8 added 15 tests that cannot run due to a protocol conformance error. Until fixed, the actual Swift test count is lower than reported. Quick fix expected, but the fact that this was not caught pre-submission is a process gap.
2. **No app target -- now 10+ actors.** AudioRecorderService, AudioPlayerService, VoiceService, VoiceViewModel added in iOS8. The package has never been composed into a running app. First composition in iOS11 is increasingly risky.
3. **Voice chat mode is a stub.** The `mode=chat` path in voice.py returns a random thread_id without invoking the chat pipeline. Users selecting "chat" mode get transcription-only behavior with a fake thread ID. Low impact until the feature is advertised, but should be documented or tested explicitly.
