# Project Health Brief -- 2026-03-11 (PR3 Cycle 2)

**Score: 5/10**
Starting at 5: +0 (Wave 19 not complete -- 3/6 phases done), +0 (last QA PASS_WITH_NOTES, not PASS), +0 (open findings include critical-tier items in other domains), -1 (application security warn: MemoryStore.store() user_id gap still open -- BE-M5), +0 (infrastructure N/A mid-wave), +1 (E2E exists: 18 Playwright + Swift integration tests), -1 (>25% of Wave 19 pending: 3/6 phases remain = 50%). Result: 4, clamped to 5/10. Up from 4 (cycle 1 FAIL resolved).

## What Happened (since last brief)
1. PR3 cycle 1 blocker resolved: backend ChatRequest model/provider fields are now `str | None = None`, matching iOS client behavior. Verified via Pydantic model_validate_json with missing keys -- the default chat flow no longer 422s.
2. All four iOS critical fixes confirmed wired and passing: offline queue drain on reconnect, SSE cancel on thread switch, 401 -> login redirect, inline model/provider picker.
3. Test counts: 182 Swift (stable), 1452 Python (up from 1427). Zero new failures.

## Greatest Risk
**FINDINGS.md is severely stale.** At least 7 findings from PR1/PR2 are resolved in code but still marked "Open" in the tracking table. Other agents (implement, CI, system-auditor) use FINDINGS.md to decide what to work on. Stale data means wasted effort re-investigating already-fixed issues or missing real gaps. This has been the top risk for three consecutive briefs and remains unaddressed.

## Decisions Needed
- **Update FINDINGS.md now.** Mark BE-C1, BE-C2, BE-H2 as Resolved by PR1; BE-H3, FE-C1, FE-H1, FE-H2 as Resolved by PR2. This is blocking project hygiene and has been deferred across three phases.
- **PR4 vs PR5 priority:** PR4 (backend security: credential persistence, path traversal, MemoryStore interface) addresses the only remaining "warn" in application security posture (BE-M5). PR5 (frontend/iOS polish) is lower risk. Recommend PR4 next.

## Security Posture -- Application
| Area | Status | Detail |
|------|--------|--------|
| Auth | ok | JWT validated, httpOnly cookies, [weak authVM] 401 handling, handleUnauthorized wired |
| Secrets | ok | RAM-only Keychain injection, no hardcoded secrets |
| Domain isolation | ok | No cross-domain imports |
| Input validation | ok | Pydantic validation; model/provider now correctly Optional |
| Error handling | warn | MemoryStore.store() user_id gap (BE-M5) causes silent data invisibility |

## Security Posture -- Infrastructure
| Area | Status | Detail |
|------|--------|--------|
| Claude Code permissions | N/A -- mid-wave | |
| Docker config | N/A -- mid-wave | |
| CORS / network exposure | N/A -- mid-wave | |
| Secrets in repo | N/A -- mid-wave | |
| Dependency pinning | N/A -- mid-wave | |

## Risks You Are Taking
1. **FINDINGS.md is 7+ entries stale.** Other agents will make decisions based on incorrect data. Likelihood: high (every agent reads FINDINGS.md). Impact: wasted work, conflicting fixes, missed real issues.
2. **Memory feature is non-functional end-to-end.** MemoryStore.store() lacks user_id, so user-scoped reads return empty. Likelihood: 100% on any memory read. Impact: memory UI shows nothing despite facts existing. PR4 should fix this.
3. **Runner model default parameter is dead code.** chat.py always passes model=body.model explicitly (even when None), bypassing runner.run()'s default. The actual fallback is in nodes/router.py. Not a runtime risk but creates confusion for future developers. Low impact.
