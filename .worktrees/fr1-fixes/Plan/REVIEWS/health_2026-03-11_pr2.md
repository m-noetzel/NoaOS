# Project Health Brief -- 2026-03-11 (PR2)

**Score: 5/10**
Starting at 5: +0 (Wave 19 not complete -- 2/6 phases done), +0 (last QA PASS_WITH_NOTES, not PASS), +0 (open findings include critical-tier items in other domains), -1 (application security warn: MemoryStore.store() user_id gap still open -- BE-M5), +0 (infrastructure N/A mid-wave), +1 (E2E exists: 18 Playwright + Swift integration tests), -1 (>25% of Wave 19 pending: 4/6 phases remain). Result: 4, clamped to 5/10. Stable from previous brief.

## What Happened (since last brief)
1. PR2 delivered three bug fixes: PATCH settings endpoint (PrivacyToggle no longer silently fails), Chat thread creation race eliminated (mutateAsync + null-guard), RunDetail unsafe type cast removed.
2. FINDINGS.md is now 7 findings behind reality -- PR1 resolved 3 (BE-C1, BE-C2, BE-H2) and PR2 resolved 4 (BE-H3, FE-C1, FE-H1, FE-H2), but none are updated in the tracking table yet.
3. Frontend test suite stable at 92/94 (2 pre-existing failures in QC7 settings freshness tests).

## Greatest Risk
**FINDINGS.md drift.** Seven findings are resolved in code but still marked "Open" in the tracking table. This is the most dangerous project-level risk right now because other agents (implement, CI, system-auditor) use FINDINGS.md to decide what to work on. If they read stale data, they will either re-fix already-fixed issues or miss the real remaining gaps. The fix is trivial (update 7 rows in a table) but keeps getting deferred. Every phase that passes without updating FINDINGS.md increases the risk of wasted work or conflicting changes.

## Decisions Needed
- **Update FINDINGS.md immediately.** Mark BE-C1, BE-C2, BE-H2 as Resolved by PR1. Mark BE-H3, FE-C1, FE-H1, FE-H2 as Resolved by PR2. This is blocking project hygiene.
- **Prioritize PR3 vs PR4:** PR3 (iOS fixes) and PR4 (backend security) are independent. The MemoryStore user_id gap (BE-M5) in PR4 is the biggest functional bug remaining. Consider doing PR4 first.

## Security Posture -- Application
| Area | Status | Detail |
|------|--------|--------|
| Auth | ok | JWT validated, httpOnly cookies, PATCH endpoint requires auth |
| Secrets | ok | RAM-only Keychain injection, no hardcoded secrets |
| Domain isolation | ok | No cross-domain imports |
| Input validation | ok | Pydantic validation on settings request body |
| Error handling | warn | MemoryStore.store() user_id gap (BE-M5) causes silent data invisibility |

## Security Posture -- Infrastructure
| Area | Status | Detail |
|------|--------|--------|
| Claude Code permissions | N/A -- mid-wave | |
| Docker config | N/A -- mid-wave | |
| CORS / network exposure | N/A -- mid-wave | |
| Secrets in repo | N/A -- mid-wave | |
| Dependency pinning | N/A -- mid-wave | |

## Risks You Are Taking
1. **FINDINGS.md is 7 entries stale.** Other agents will make decisions based on incorrect data. Likelihood: high (every agent reads FINDINGS.md). Impact: wasted work, conflicting fixes, missed real issues.
2. **Memory feature is non-functional end-to-end.** MemoryStore.store() lacks user_id, so user-scoped reads return empty. Likelihood: 100%. Impact: memory UI shows nothing despite facts existing.
3. **No non-mocked integration test for PATCH settings.** The fix is verified via mocked sessions. A real ASGI test would catch wiring or serialization issues that mocks hide. Likelihood of latent bug: low (settings service is simple). Impact: PrivacyToggle could silently fail in production if wiring is wrong.
