# Project Health Brief -- 2026-03-11 (PR4)

**Score: 5/10**
Starting at 5: +0 (Wave 19 not complete -- 4/6 phases done), +0 (last QA PASS_WITH_NOTES, not PASS), +0 (open findings include high-tier items), -1 (application security warn: chat.py str(exc) leak to client, MemoryStore.store() user_id gap BE-M5 still open), +0 (infrastructure N/A mid-wave), +1 (E2E exists: 18 Playwright + Swift integration tests), -1 (>25% Wave 19 pending: 2/6 = 33%). Result: 4, clamped to 5/10. Stable from PR3.

## What Happened (since last brief)
1. PR4 resolved three audit findings: BE-H1 (credential persistence + ProviderRouter hot-reload), BE-M3 (path traversal guard in artifact download), BE-M4 (structured log context with user_id/run_id/trace_id in orchestrator and chat).
2. Runner.py error events now send generic messages to clients instead of raw str(exc), closing a potential information disclosure path.
3. Default model changed from "anthropic/claude-haiku" to "openai/gpt-4.1-mini" across router.py, agent.py, runner.py, and test_mr8.

## Greatest Risk
**FINDINGS.md is severely stale.** This is the fifth consecutive brief flagging this issue. At least 10 findings (BE-H1, BE-M3, BE-M4 from PR4, plus 7 from PR1/PR2 flagged in earlier briefs) are resolved in code but still marked "Open" in FINDINGS.md. Other agents use this file to decide what to work on. The longer this drifts, the higher the risk of wasted effort re-investigating fixed issues or missing actual gaps. This must be addressed before Wave 19 closes.

## Decisions Needed
- **Batch-update FINDINGS.md now.** Mark all resolved findings from PR1 through PR4. The list has been documented in every health brief since PR1. This is blocking project hygiene.
- **PR5 scope confirmation:** PR5 targets frontend/iOS polish (online indicator, React Router redirect, auth'd artifact download, credential validation, iOS lifecycle). Proceed as planned?

## Security Posture -- Application
| Area | Status | Detail |
|------|--------|--------|
| Auth | ok | httpOnly cookies, JWT validated, no empty fallback |
| Secrets | ok | Credentials persisted to DB, env-var fallback only for API keys not in DB. No hardcoded secrets. |
| Domain isolation | ok | No cross-domain imports confirmed |
| Input validation | ok | Path traversal guard added (BE-M3). Email validation (QC2). Schema validation on all endpoints. |
| Error handling | warn | runner.py fixed (generic error to client). chat.py:157-162 still leaks str(exc) via SSE (pre-existing CP3). MemoryStore.store() still missing user_id (BE-M5). |

## Security Posture -- Infrastructure
N/A -- mid-wave. Full audit at Wave 19 boundary.

| Area | Status | Detail |
|------|--------|--------|
| Claude Code permissions | N/A | mid-wave |
| Docker config | N/A | mid-wave |
| CORS / network exposure | N/A | mid-wave |
| Secrets in repo | N/A | mid-wave |
| Dependency pinning | N/A | mid-wave |

## Risks You Are Taking
1. **FINDINGS.md drift (high likelihood, medium impact):** 10+ findings are resolved but unmarked. Agents relying on this file will waste cycles. Fix is trivial but has been deferred across 5 reviews.
2. **chat.py str(exc) SSE leak (low likelihood, medium impact):** If the outer exception handler in chat.py catches an error containing sensitive info (DB connection string, API key in error message), it would be sent to the client via SSE. The runner.py inner handler was fixed; this outer handler was not. Mitigated by the fact that most errors are caught by the inner handler first.
3. **MemoryStore.store() missing user_id (BE-M5, medium likelihood, low impact):** Facts stored via the orchestrator have no user_id set, making them invisible to user-scoped API queries. Since Noa is single-user, this is low impact now but would become a data leak vector if multi-user support is ever added.
