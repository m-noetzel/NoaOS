# Project Health Brief -- 2026-03-11 (PR5)

**Score: 5/10**
Starting at 5: +0 (Wave 19 not complete -- 5/6 phases done), +0 (last QA PASS_WITH_NOTES, not PASS), +0 (open findings include high-tier BE-H4/BE-H5), -1 (application security warn: ErrorBoundary stack trace exposure, MemoryStore.store() BE-M5 user_id gap still open), +0 (infrastructure N/A mid-wave), +1 (E2E exists: 18 Playwright + Swift integration tests). Result: 5, clamped to 5/10. Stable from PR4.

## What Happened (since last brief)
1. PR5 resolved 9 medium-severity findings across frontend and iOS: real online indicator (FE-M1), React Router session redirect (FE-M2), authenticated artifact download (FE-M3), credential validation (FE-M4), MainTabView lifecycle cleanup (iOS-M1), loadHistory race fix (iOS-M2), biometric retry UI (iOS-M3), batch deny confirmation (iOS-M4), voice cancel/timeout (iOS-M5).
2. FINDINGS.md is now up to date: 10 open, 90 resolved (100 total). The stale-findings problem flagged in every brief since PR1 appears resolved.
3. Only PR6 (integration tests) remains before Wave 19 closes.

## Greatest Risk
**No integration tests for the fixes made in PR1-PR5.** The entire Wave 19 has been code fixes without corresponding end-to-end verification. Each phase passed QA with unit-level tests and mocks, but none of the fixed flows (chat-to-cost pipeline, memory user isolation, thread CRUD, settings round-trip, authenticated artifact download, session expiry redirect) have been tested against the running system. PR6 is the designated phase for this, and it is the single most important remaining deliverable. If PR6 is skipped or thin, Wave 19's fixes exist only in theory.

## Decisions Needed
- **PR6 scope prioritization.** PR6 is planned for both Python and Swift integration tests. Given time pressure, decide whether to focus on Python tests (more findings to verify) or split evenly. Recommend prioritizing: (1) chat-to-cost data flow, (2) memory user isolation, (3) authenticated artifact download, (4) session expiry redirect.

## Security Posture -- Application
| Area | Status | Detail |
|------|--------|--------|
| Auth | ok | httpOnly cookies, registerSessionExpiredHandler with React Router navigate, cleanup on unmount |
| Secrets | ok | No hardcoded secrets. CredentialModal rejects empty keys. Credential store persistence (PR4) |
| Domain isolation | ok | grep confirmed no cross-domain imports |
| Input validation | ok | CredentialModal trim+reject. Budget validation (QC6). Email validation (QC2) |
| Error handling | warn | ErrorBoundary.tsx:43 exposes error.stack to users (pre-existing from QC6). Artifact download failure silently logged (no user feedback) |

## Security Posture -- Infrastructure
N/A -- mid-wave (not a wave boundary).

| Area | Status | Detail |
|------|--------|--------|
| Claude Code permissions | N/A | mid-wave |
| Docker config | N/A | mid-wave |
| CORS / network exposure | N/A | mid-wave |
| Secrets in repo | N/A | mid-wave |
| Dependency pinning | N/A | mid-wave |

## Risks You Are Taking
1. **No integration tests for Wave 19 fixes (high impact, medium likelihood).** All PR1-PR5 fixes verified via unit tests only. If a wiring issue exists between components, it won't surface until PR6 or production. Likelihood is medium because individual fixes are small and well-scoped, but impact is high because data integrity issues (BE-C1, BE-C2) could go undetected.

2. **ErrorBoundary stack trace exposure (low impact, high likelihood).** Any React error in production will show component file paths to the user. Low security impact (no secrets, just internal paths) but unprofessional. Has been open since QC6.

3. **BE-H4 and BE-H5 remain open (medium impact, low likelihood).** SSE replay cursor uses list index (reconnection unreliable) and chat.py bypasses RunService state machine. Both are correctness issues that affect edge cases, not normal flows.
